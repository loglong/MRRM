# Development Dockerfile with Hot Reload for React Frontend
FROM node:20-alpine

WORKDIR /app

# Install dependencies
COPY package*.json ./
RUN npm ci

# Copy source code
COPY . .

# Expose port
EXPOSE 5173

# Start with hot reload using Vite dev server
CMD ["npm", "run", "dev", "--", "--host", "--port", "5173"]
